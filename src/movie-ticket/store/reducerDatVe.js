const stateDefault = {
    danhSachGheDangDat: [],
};

const DatveReducer = (state = stateDefault, action) => {
    switch(action.type) {
        default: return {...state}
    }
};
 export default DatveReducer;